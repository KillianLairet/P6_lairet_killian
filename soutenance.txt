Exigences concernant la sécurité :

1.

2.refTo: 'controllers/user' -> 'bcrypt.hash(req.body.password, 10)'

3.

4.

5.refTo: 'middleware/auth'          ->      'const decodedToken = jwt.verify(token, 'RANDOM_TOKEN_SECRET')'
            +'controllers/user'             +'const token = jwt.sign()'
            +'routes/sauce'                 +'auth'

6.refTo: 'controllers/user' -> 'const user = new User({ email: req.body.email, password: hash })'

7.refTo:'models/User' -> 'unique: true' & 'userSchema.plugin(uniqueValidator)'

